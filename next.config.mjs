/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Kindergarten-web",
  assetPrefix: "/Kindergarten-web/",
  images: {
    unoptimized: true
  }
};

export default nextConfig;
